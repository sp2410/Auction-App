<% if (@product.has_auction?) %>
  <% if (@product.originaluser == current_user) %>    
    
    <%= form_for [ @product, @product.auction, CounterOffer.new ] do |f| %>    
        
        <%= f.text_field :value %>      
        <%= f.button "Place Counter Offer", class: "btn btn-primary"  %>      

    <% end %> 


  <% end %>  
<% end %>

